server {
    listen 80;
    server_name searxng.tabca.vn;

    # Redirect to https if desired
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 10s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Buffers
        proxy_buffer_size 16k;
        proxy_buffers 4 32k;
        proxy_busy_buffers_size 64k;
    }
}

# Optional TLS block (uncomment after obtaining certificates)
#server {
#    listen 443 ssl http2;
#    server_name searxng.tabca.vn;
#
#    ssl_certificate /etc/letsencrypt/live/searxng.tabca.vn/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/searxng.tabca.vn/privkey.pem;
#    ssl_protocols TLSv1.2 TLSv1.3;
#
#    location / {
#        proxy_pass http://127.0.0.1:8081;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#        proxy_connect_timeout 10s;
#        proxy_send_timeout 60s;
#        proxy_read_timeout 60s;
#    }
#}
