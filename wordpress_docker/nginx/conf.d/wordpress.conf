server {
    listen 80;
    server_name wordpress.tabca.vn;

    # redirect to HTTPS if you will enable TLS
    # return 301 https://$host$request_uri;

    client_max_body_size 200M;        # adjust as needed
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        proxy_http_version 1.1;
        proxy_set_header Connection "";

        # avoid backend redirects to internal urls
        proxy_redirect off;

        # timeouts
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # TLS block example (uncomment after you provision certs)
    #server {
    #    listen 443 ssl http2;
    #    server_name wordpress.tabca.vn;
    #    ssl_certificate /etc/letsencrypt/live/wordpress.tabca.vn/fullchain.pem;
    #    ssl_certificate_key /etc/letsencrypt/live/wordpress.tabca.vn/privkey.pem;
    #
    #    client_max_body_size 200M;
    #
    #    location / {
    #        proxy_pass http://127.0.0.1:8082;
    #        proxy_set_header Host $host;
    #        proxy_set_header X-Real-IP $remote_addr;
    #        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #        proxy_set_header X-Forwarded-Proto $scheme;
    #        proxy_set_header X-Forwarded-Host $host;
    #        proxy_buffering off;
    #        proxy_redirect off;
    #    }
    #}
}
